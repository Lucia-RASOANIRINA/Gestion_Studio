# @gestion-studio/shared

Types, enums et constantes partagés entre `apps/api` et `apps/web` (modules de permissions, statuts de projet, devises, locales supportées, etc.).

## État

Scaffolding initial. À enrichir phase par phase avec les DTOs/interfaces métier au fur et à mesure de leur implémentation côté API.
