# Module Paramètres & Configuration

Infos entreprise (NIF/STAT/RCS), devises, taxes, numérotation automatique, langues, notifications, rôles/permissions, types de services/tarifs, modèles de documents/contrats, workflows personnalisables, webhooks/API sortante (voir section 4.7).

## État

Scaffolding uniquement (route `GET /api/settings` protégée par permission, réponse stub). Logique de configuration à implémenter en phase 8.
